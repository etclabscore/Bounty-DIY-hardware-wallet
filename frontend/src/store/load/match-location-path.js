// import { getMatchedUrl } from 'utils/location';

export default Base =>
  class extends Base {
    async matchLocationPath() {
      // const matchedUrl = getMatchedUrl();
      switch (true) {
        default:
      }
    }
  };
